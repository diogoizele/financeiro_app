import styled from "@emotion/styled";

export const ActivityHeaderContainer = styled("header")(() => ({
  width: "100%",
  height: "fit-content",
  display: "flex",
  justifyContent: "space-between",
  alignItems: "center",
  padding: "0 1rem",
}));
